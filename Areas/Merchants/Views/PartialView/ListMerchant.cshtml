@model List<Todoku.Models.Merchant>

<ul class="dropdown-menu">
    @foreach (var item in Model) 
    { 
        <li>@Html.ActionLink(@item.MerchantName, "Index", "Home", new { area = "Merchants", DashboardID = TempData.Peek("DashboardID"), MerchantID = @item.MerchantID }, null)</li>
    }
</ul>